# Copy to .env and fill in. .env is gitignored — never commit real keys.
OPENROUTER_API_KEY=
# Optional: any model id from openrouter.ai/models. Leave empty to use the
# default in spine/llm.py (verified on first live call).
OPENROUTER_MODEL=
